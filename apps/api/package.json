{
  "name": "@central-command/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@central-command/db": "workspace:*",
    "@central-command/types": "workspace:*",
    "@central-command/utils": "workspace:*",
    "drizzle-orm": "^0.44.2",
    "hono": "^4.8.3",
    "uuid": "^14.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.20.5"
  }
}
